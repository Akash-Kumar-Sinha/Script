const ChatBar = () => {
  return (
      <div className=" bg-gray-100 h-screen text-2xl flex justify-center items-center">
        Select a Chat to start a conversation
      </div>
  );
};

export default ChatBar;
