import React from 'react'
import LoadingModal from './LoadingModal'

const Loading = () => {
    console.log("need to figure out loading model")
  return (
   <LoadingModal/>
  )
}
export default Loading